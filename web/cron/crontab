SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# Disabled in multi-user mode — re-enable after porting organize.sh to iterate
# pending raws per owner via internal-token + X-Haera-Owner-Id headers.
# * * * * * root /opt/haera/organize.sh >> /var/log/haera.log 2>&1
